import AuthContainer from "../_/components/AuthContainer";

export default function AuthPage() {
  return (
    <section className="flex items-center justify-center h-screen">
      <AuthContainer />
    </section>
  );
}
